# 10 — Задачи для ClickUp (черновик, вставлять по одной)

> Каждый блок — одна задача: заголовок + описание. Детали агент исследует сам; здесь только цель, ссылки, границы и критерий.
> Зависимости — в поле «После». Общий блок ниже вставляется в начало каждой задачи (или в шаблон списка).

## Общий блок (в начало каждой задачи)

```
Контекст: целевая архитектура KB Labs — docs/architecture/target/ (01–09). Этап миграции — 07-migration.md.
Правила: AGENTS.md и CLAUDE.md; без хаков, причина а не симптом; релизы только по явному разрешению автора;
bug fix — с регрессионным тестом; новые коды ошибок — в каталог (08-errors.md).
```

---

# Этап 0 — Ограждения

### 0.1 Файл резерва namespace и проверки
Цель: зарезервировать имена команд до появления сторонних плагинов.
Docs: 06-command-naming.md §7–8.
Сделать: единый файл `reserved-namespaces` в `cli/contracts` (уровни S/V/F/R); проверка в линте манифеста (`kb-devkit`), при `plugin install/enable` и в registry при регистрации.
Не делаем: правила владельца namespace по скоупу (этап 7).
Готово: манифест с зарезервированным namespace отклоняется с понятной ошибкой и подсказкой замены; тесты на 4 уровня.

### 0.2 Линты границ
Цель: новые нарушения границ не появляются.
Docs: 05-executables.md §3, `docs/ARCHITECTURE-BOUNDARIES.md`.
Сделать: правила в `kb-devkit`: (а) плагины не импортируют `core-*` в обход SDK, (б) `services/*`, `studio/*`, `plugins/*/daemon` не запускают бинарники платформы через child_process. Существующие нарушения — в явный список исключений с датой.
Готово: линт в CI, список исключений закоммичен.

### 0.3 Платформа — peerDependency плагина
Цель: закрыть баг «плагин тянет свою копию платформы».
Docs: 03-domain-model.md (модель расширений), 07 этап 0.
Сделать: линт «`@kb-labs/*` платформенные пакеты у плагина только в `peerDependencies`»; исправить шаблоны `templates/plugin-template`.
Готово: линт в CI; шаблон проходит; в первых-party плагинах нарушений нет либо они в списке исключений.

### 0.4 Единый конверт ошибки и каталог кодов
Цель: одна форма ошибки для лаунчера (Go), платформы (TS), Studio.
Docs: 08-errors.md, ADR-0033.
Сделать: схема конверта в `contracts`; `errors.catalog.json`; общий набор фикстур для Go и TS; адаптер для существующих TS-ошибок (`PluginError`, `ValidationError`, `ServiceNotConfiguredError`, `AdapterUnavailableError`); переименовать `KB_CREATE_*` → `KB_INSTALL_*`; линт «код есть в каталоге и имеет hint».
Готово: `kb-create` и `kb` выдают ошибки в одном конверте; тест совместимости Go↔TS; каталог из 08-errors.md заведён данными.

### 0.5 Линт именования команд
Цель: единый стиль команд.
Docs: 06-command-naming.md §1–3.
Сделать: проверка манифестов: глагол из словаря, регистр/kebab, у `operationType: mutate` есть `--dry-run` и `--json`.
После: 0.1.
Готово: линт в CI; существующие отклонения исправлены или в исключениях.

### 0.6 Индекс архитектурных документов
Цель: контрибьюторы и агенты находят карту.
Сделать: `docs/architecture/README.md` с картой `target/` и ссылками на ADR; две строки «начни отсюда» в `AGENTS.md`.
Готово: ссылки живые; `docs` staleness-проверка (ADR-0023) не ломается.

---

# Этап 1 — Реестр проектов

### 1.1 Реестр проектов (библиотека)
Цель: проект — сущность платформы.
Docs: 04-topology.md §5 (п.4, 7), 03-domain-model.md.
Сделать: ADR-0044; модуль в `core`: `~/.kb/projects.json`, `projectId` от абсолютного пути, статус, последний запуск; каталог состояния `~/.kb/state/<projectId>/`; атомарная запись + блокировка файла.
Не делаем: перенос проекта между путями (перенесённая папка = новый проект).
Готово: API add/list/get/remove с тестами на параллельную запись.

### 1.2 Команды `kb project add | list | show | remove`
Docs: 06-command-naming.md.
Сделать: системные команды поверх 1.1; `add` на папке с `.kb/` подхватывает её, без `.kb/` — предлагает инициализацию; `--json`, `--dry-run` для мутирующих; коды ошибок из 08 (область D).
После: 1.1, 0.4.
Готово: два проекта зарегистрированы и видны в `kb project list`.

### 1.3 Рантайм-состояние проекта — вне репозитория
Цель: убрать шум в `git status` проекта.
Сделать: инвентаризация записей в `<repo>/.kb/` (cache, logs, lock рантайма, tmp); перенос в `~/.kb/state/<projectId>/`; в декларации остаётся только конфиг.
После: 1.1.
Готово: после работы в проекте `git status` не показывает рантайм-файлов.

---

# Этап 2 — Хост v0

### 2.1 Спайк: один хост-процесс из нескольких сервисов
Цель: проверить модель B на реальном коде до основной работы.
Docs: 04-topology.md §1, §3.
Известно: `launchPlatform` не допускает второе приложение в процессе (`core/runtime/src/platform-launch.ts`), `platform` — process-синглтон, сервисы стартуют через `runService`/`setup()` (`shared/daemon/src/daemon.ts`).
Сделать: прототип: один `launchPlatform`, в нём `setup()` gateway + marketplace + state. Письменные выводы: что мешает, что менять.
Готово: короткий отчёт + ADR-0043 (draft) с решением; если не работает — вернуть в 04.

### 2.2 Bootstrap хоста (`kb-host`)
Сделать: единая точка сборки модулей машинного уровня (gateway, marketplace, state, studio-static) без переписывания их кода.
После: 2.1.
Готово: один процесс поднимает все четыре модуля; общий health и статус.

### 2.3 Gateway отдаёт Studio, один внешний порт 4000
Docs: 04 §5 (п.2).
Сделать: `/` — статика Studio, `/api/v1` — gateway; внутренний транспорт за `IServiceTransport`.
После: 2.2. Заметка: проверить, нет ли у Studio серверной части (`studio/app/server.js`).
Готово: `localhost:4000` открывает Studio; порты 3000/5070/7777 снаружи не нужны.

### 2.4 Режим авторизации при установке и политика bind
Docs: 04 §5 (п.5), ADR-0020.
Сделать: режим «с авторизацией / без» как свойство установки; без неё — только loopback; наружу — только с ней; ошибка `KB_HOST_EXPOSURE_REFUSED`.
Готово: попытка выставить хост без авторизации наружу отказывает понятной ошибкой; тест.

---

# Этап 3 — Рантайм проекта

### 3.1 Процесс рантайма проекта
Цель: проектные сервисы (rest, workflow, mcp) — по процессу на активный проект, лениво.
Docs: 04 §2–3.
Сделать: рантайм проекта собирает `setup()` rest/workflow/mcp в один процесс со своим `launchPlatform` и `projectRoot`; хост запускает при первом обращении.
После: 2.2, 1.1.
Готово: два проекта одновременно, у каждого свой рантайм.

### 3.2 Динамические адреса и маршрутизация по проекту
Сделать: `serviceTransport` умеет динамически регистрировать адрес рантайма; gateway маршрутизирует `/api/v1/projects/{id}/…`.
После: 3.1.
Готово: плагин коммитов работает в обоих проектах через один порт.

### 3.3 Остановка по простою и лимит активных рантаймов
Готово: неактивный рантайм гаснет; при лимите — `KB_PROJECT_RUNTIME_LIMIT`; нет зависших процессов после остановки хоста.
После: 3.1.

### 3.4 Убрать `--net-offset` из пользовательского пути
Сделать: пользовательский запуск не требует ручных портов и офсета; они остаются у `kb-dev` для разработки платформы.
После: 3.2.
Готово: старт без `--net-offset`/`--config`.

---

# Этап 4 — Лаунчер-супервизор

### 4.1 `kb-create start | stop | status` и автозапуск хоста
Docs: 05-executables.md.
Готово: старт/останов/статус хоста; автозапуск на macOS/Linux/Windows; `kb start|stop|status` — единственный алиас на лаунчер.
После: 2.2.

### 4.2 Control-канал лаунчера
Docs: 05 §3 (правило 2), §5.
Сделать: loopback + токен, версионированный JSON-протокол; endpoint хоста `/api/v1/platform/update-request` → канал.
Не делаем: сокеты/pipes.
Готово: Studio-заглушка запрашивает обновление через хост; хост никогда не запускает бинарники сам; контрактный тест.
После: 4.1.

### 4.3 update/rollback с перезапуском хоста; `doctor` и `doctor --recover`
Сделать: обновление останавливает и запускает хост; автооткат при `KB_UPDATE_HOST_NOT_HEALTHY`; `doctor` различает установку и платформу; `reset-admin` перенести из `cli/commands/.../auth` в `doctor --recover`.
После: 4.1, 0.4.
Готово: сценарий «обновление сломало хост» откатывается сам.

### 4.4 Убрать `kb-dev` из пользовательского запуска
Сделать: пользовательский режим не использует `devservices.yaml`; конфиг хоста генерируется установщиком.
После: 4.1, 2.2.
Готово: пользователь не знает о `kb-dev`; разработка платформы работает как раньше.

---

# Этап 5 — Конфиг

### 5.1 Разделение конфига: генерируемое и пользовательское
Docs: 04 решение 9.
Сделать: установщик пишет только в `.kb/generated/…`; пользовательский `kb.config.jsonc` пишет только `config set`; правило «один писатель на файл».
Готово: `kb-create update` не затирает пользовательские правки.

### 5.2 `kb config get | set | show`
Сделать: клиент хоста; работает и напрямую по файлу, когда хост выключен (одна библиотека `core-config`); атомарная запись, блокировка, валидация по схеме; ошибки области E.
После: 5.1, 0.4.
Готово: настройка адаптера и ключа LLM без ручного JSON.

### 5.3 Схемы секций конфига платформы
Сделать: сейчас строго валидируется только `ExecutionConfig` (`core/runtime/src/config-schemas.ts`); добавить схемы для остальных секций; у адаптеров `configSchema` уже есть.
Готово: неверное значение даёт `KB_CONFIG_INVALID` с путём поля.

### 5.4 Топология и URL сервисов — из плана установки
Сделать: `adapterOptions.serviceTransport.services` и подобное больше не пишутся руками; генерируются.
После: 5.1, 2.3.
Готово: `kb.config.jsonc` пользователя не содержит адресов сервисов.

---

# Этап 6 — Studio

### 6.1 Экран проектов
Сделать: список, `+ Add project`, переключение, статус рантайма. Все действия — операции хоста, эквивалентные командам `kb project *`.
После: 1.2, 3.2.

### 6.2 Плагины и адаптеры
Сделать: включить/выключить на проекте; форма опций адаптера из `configSchema`; ввод ключа LLM.
После: 5.2, 5.3.

### 6.3 Health, doctor и «Обновить»
Сделать: статус модулей, вывод ошибок в едином конверте (кнопки из `actions`), кнопка обновления через API хоста → control-канал.
После: 4.2, 0.4.

### 6.4 `kb plugin` и `kb adapter` как системные клиенты хоста
Docs: 06 §5, §7.
Сделать: заменить `marketplace plugins …` на `kb plugin …`/`kb adapter …` (системные команды, клиент хоста); старые имена удалить.
После: 0.1, 2.2.

---

# Этап 7 — Модель расширений (отдельный рискованный срез)

### 7.1 Спайк и ADR-0048
Docs: 03-domain-model.md (вариант C), ADR-0012, ADR-0030.
Сделать: исследовать discovery, marketplace lock, резолв модулей; письменный план и ADR.
Готово: ADR принят автором (спайк выполнен: решения D1–D10 в ADR-0048, порядок 7.6 → 7.2 → 7.7 → 7.3 → 7.4 → 7.5).

### 7.6 Единый discovery по lock (новая, L)
Docs: ADR-0048 (F1, D4), ADR-0012 §6.
Сделать: CLI-discovery (`cli/commands/src/registry/discover.ts`) перестаёт сканировать node_modules/workspace и берёт плагины из lock через общий резолвер в `core-discovery`; workspace-плагины входят как `link`-записи; единый переключатель enable (убрать `plugins.allow` и enabled/disabled из `.kb/plugins.json`).
Не делаем: хранилище и пины (7.2/7.3).
Готово: CLI, REST и адаптеры видят один и тот же набор плагинов; e2e roots-спеки зелёные.
После: 7.1.

### 7.2 Общее хранилище версий (M)
Docs: ADR-0048 D2.
Сделать: `$KB_HOME/store/entries/<name>@<version>/` с `entry.json`, `store.lock`, отдельный pnpm CAS, атомарная установка через staging, integrity, `kb plugin gc`, установка по статическому манифесту без импорта JS; коды `KB_PLUGIN_INTEGRITY_MISMATCH`.
Не делаем: платформенные плагины и адаптеры остаются как есть.
Готово: одна копия версии на машину; замер дедупликации на плагине из registry и прогон на Windows.
После: 7.6.

### 7.7 Единственная копия платформы (новая, M)
Docs: ADR-0048 D5.
Сделать: resolve-хук (`module.registerHooks`) для платформенных пакетов в worker-script, subprocess-бутстрапе, `cli/bin` и демонах; `KB_PLUGIN_PLATFORM_DUPLICATE`; тесты с двумя версиями контракта SDK в одном процессе.
Готово: реальный плагин (например `commit-entry`) работает в worker-pool через хук; это гейт перед 7.3.
После: 7.2.

### 7.3 Пин версии в проекте (lock проекта) (L)
Docs: ADR-0048 D3, D4, D8.
Сделать: схема `kb.marketplace/3` (читатель/писатель в `core-discovery`, Go-писатель `tools/kb-create/v2/marketplace/lock.go`, `check-marketplace-lock.mjs`, helm/docker); установка в проект больше не делает `pnpm add` в репозитории; `kb plugin install/update`; adapters только в platform lock; коды `KB_PLUGIN_LOCK_INVALID`, `KB_PLUGIN_STORE_MISSING`, `KB_ADAPTER_PROJECT_SCOPE`.
Готово: проект пинует версию и включает; два проекта на разных версиях одного плагина.
После: 7.7.

### 7.4 Проверка версии SDK-контракта
Сделать: `SDK_API_VERSION` в `plugin-contracts` (независим от npm-версии), поле `sdkApiVersion` (semver-диапазон) в `ManifestV3` и в `dist/manifest.json`; проверка при установке (по статическому манифесту) и при загрузке; `KB_PLUGIN_SDK_INCOMPATIBLE`, `KB_PLUGIN_MANIFEST_INVALID`; гейт `check-api-removals`; метаданные версий в registry. Отдельно (S–M): убрать 34 исключения `plugin-platform-dependency`.
Размер: M.
После: 7.1, 0.3.

### 7.5 Владелец namespace по скоупу; затенение — ошибка при установке
Docs: 06 §7.
Сделать: чистая функция владельца (проект > платформа, ничья — ошибка) для установщика и реестра; проверки в `marketplace-core` до записи lock (`KB_PLUGIN_NAMESPACE_RESERVED`/`TAKEN`); реестр перестаёт сортировать по `SOURCE_PRIORITY`; видимость затенения в `kb health`/`kb plugin doctor`.
Размер: M.
После: 7.3.

---

# Этап 8 — Оболочка

### 8.1 Мастер установки поверх JSON-протокола `kb-create`
Docs: ADR-0028, 02-ux-journey.md.
Сделать: окно с тремя вопросами (режим, набор модулей, каталог), прогресс, «Открыть KB Labs»; без логики установки в оболочке.
После: 4.1.

### 8.2 Трей и автозапуск
Сделать: иконка, статус хоста, «Открыть Studio», «Обновить».
После: 8.1, 4.2.

---

# Этап 9 — Первая польза

### 9.1 Плагин коммитов из коробки
Сделать: включён по умолчанию на добавленном проекте; сценарий «первая польза ≤5 минут».
После: 6.1.

### 9.2 «Создать свой плагин» в Studio
Сделать: шаблон `templates/plugin-template` + помощь агента (плагин `agents`); по границе: платформа не знает про агентов.
После: 9.1, 0.3.

---

# Процесс релиза (R-этапы, `09-release-process.md`)

> Релизы запускать только с явного разрешения автора. Задачи не запускают релиз, а меняют процесс и проверяют его на dry-run/canary.

### R0.1 Единый отчёт о прогоне релиза
Цель: понятно, где и почему упал релиз.
Docs: 09-release-process.md §3.2, 08-errors.md.
Сделать: артефакт `release-run-report` (Markdown + JSON) из плагина `release` (`plugins/release/manager-core`): таблица стадий, классификация причины (окружение / содержимое / инфраструктура CI / нестабильность), все провалы проверок сгруппированно, hint и команда продолжения; одинаковый локально и в CI.
Готово: провал `pack-install` на любом пакете даёт отчёт с причиной, а не «exit code N»; тест на классификацию.

### R0.2 Коды ошибок релиза в общем каталоге
Docs: 09 §3.1, 08-errors.md.
Сделать: коды `KB_RELEASE_*` для всех стадий; каждая ошибка релиза в едином конверте.
После: 0.4 (конверт), R0.1.

### R1.1 Предполётная проверка (`kb release preflight`)
Docs: 09 §3.1.
Сделать: за секунды проверяет ветку, чистое дерево, Docker/Verdaccio, доступ к реестру и GitHub-токену, расхождение stable-тега и npm (baseline drift); подключён первой стадией `release-prepare`.
Готово: сломанное окружение ловится до часа проверок, с понятной причиной и подсказкой.

### R2.0 Измерить `pack-install` по фазам (СДЕЛАНО, результаты в 09 §3.3а)
Цель: подтвердить или опровергнуть гипотезу про 80 минут до перепроектирования.
Docs: 09-release-process.md §3.3а.
Сделать: тайминги по фазам на пакет (pack, статические проверки, запуск CLI, `npm install`, import) в отчёте прогона; собрать цифры на реальном прогоне на master (без публикации).
Готово: таблица «на что уходит время» в задаче; гипотеза подтверждена или заменена.

### R2.5 Перепроектировать `pack-install`
Docs: 09 §3.3а.
Сделать по результатам R2.0 (ожидаемо): статические проверки одним процессом по tarball’ам из Stage; одна установка всей платформы в один consumer + один проход импортов; отдельная чистая установка по пакету только для публичных пакетов (SDK, platform-client, поверхность авторов плагинов) и изменённых с прошлого тега; `Arborist` как библиотека, а не запуск CLI на пакет; убрать дубли с final-проверкой Stage.
После: R2.0.
Готово: время `checks` на порядок меньше; регрессионные проверки, что нарушение (workspace:-протокол, битый export, несовместимая пара) по-прежнему ловится.

### R2.1 Проверки: fail-late и полный отчёт
Сделать: `runReleaseChecks` не останавливается на первой ошибке; критичные проверки помечены `blocking`; сводка по всем пакетам.
После: R0.1.

### R2.2 Кеш по digest, resume и инкрементальный pack-install
Сделать: результат по пакету кешируется; `kb release resume <run> --from <stage>`; обычный релиз проверяет изменённые с прошлого тега пакеты и их зависимых, полный прогон — по расписанию.
После: R2.1.
Готово: повторный прогон после исправления пропускает зелёное; время типичного релиза заметно меньше.

### R2.3 Единые таймауты и явный код таймаута
Сделать: внешний бюджет и внутренняя квота из одного источника; `KB_RELEASE_CHECK_TIMEOUT` с частичным выводом.
Готово: нет «тихого» убийства процесса по внутренней квоте.

### R2.4 Waiver вместо `skipChecks`
Сделать: именованный waiver (кто, зачем, срок, что пропущено) записывается в provenance и показывается на approval.
После: R2.1.

### R2b.1 Один flow и одна версия SDK, платформы и бинарников
Docs: 09 §4а.
Сделать: один flow `release` в `release.flows` (SDK и `platform-client` перестают быть исключением платформенного flow); один релизный тег; матрица `kb.release-compatibility/2` генерируется с равными версиями; формат не меняется; тест инварианта равных версий.
Теги не переименовываем: flow остаётся `platform`, тег `platform-vX.Y.Z`; `sdk-v*` перестаём создавать (старые остаются). Проверить упоминания `sdk-v`/`platform-v` в `tools/kb-create/scripts/prepare-release-index.mjs`, `tools/kb-create/v2/cmd/kb-create-v2/main.go`, `sites/web/apps/docs/content/en/{installation,guides/ci-install}.mdx`.
Не делаем: снова разделять версии (позже, при появлении внешних потребителей SDK).
После: R0.1.
Готово: один dry-run до canary проходит по единому flow; `kb-create` проходит `CheckCompatibility` без изменений.

### R3.1 Воспроизводимость и порядок артефактов
Сделать: одна команда локального прогона проверок; ссылки на CI-логи из отчёта; `.kb/release/candidates` вне `git status` (игнор или внешнее хранилище с retention).

### R4.1 Контракт лаунчер ↔ release index
Docs: 09 §4.
Сделать: версия схемы index и отказ лаунчера с `KB_INSTALL_INDEX_SCHEMA_UNSUPPORTED`; `minLauncherVersion`; документ «кто чем штампуется» для бинарников; учесть, что этап 2 миграции меняет топологию профиля (index v3).
Готово: старый лаунчер + новый index даёт понятную ошибку и путь обновления.

### R4.2 Матрица тестов совместимости на трёх ОС
Сделать: свежая установка, обновление N-1→N и N-2→N, откат, повреждённый index, пропавший бинарник, несовпадающий SDK; запускается в CI.
После: R4.1.

### R5.1 Репетиция релиза по расписанию и статус в health
Сделать: еженедельный dry-run до canary без approval на stable; статус виден в `kb health`/Studio.
После: R0.1, R2.1.

---

# Этап 10 — Общая проверка и e2e-контур

### 10.1 Сквозной сценарий пользователя на трёх ОС
Docs: 07-migration.md этап 10, 02-ux-journey.md, 01-north-star.md (критерий успеха).
Сделать: e2e на чистых машинах (Windows, macOS, Linux; переиспользовать `kb-env`, `e2e/`, `reusable-launcher-e2e.yml`): установка → запуск хоста → добавление проекта → плагин коммитов → второй проект → обновление → откат → удаление; замер времени до первой пользы (цель ≤5 минут).
После: этапы 2–9.

### 10.2 Сценарии сбоев по каталогу ошибок
Docs: 08-errors.md.
Сделать: e2e-сценарии для мёртвого хоста, занятого порта, неверной авторизации, битой установки, несовместимого плагина, устаревшего лаунчера; проверка кода, подсказки и действия восстановления; снапшоты вывода терминала и Studio.

### 10.3 e2e-зоны хоста, проектов и расширений
Сделать: зоны для лаунчера/супервизора, хоста и gateway (режимы с/без авторизации, отказ выставить наружу без неё), нескольких проектов (изоляция состояния, лимит рантаймов), модели расширений (пины версий, несовместимость SDK, затенение имён). Добавлять только то, чего не покрывает существующий контур.

### 10.4 Studio в браузере и безопасность локального UI
Сделать: Playwright-сценарии Studio (проекты, плагины, адаптеры, health, «Обновить»); проверки Origin/Host и токена сессии локального UI.

### 10.5 Бюджеты и совместимость в CI
Сделать: пороги холодного старта, памяти хоста и рантаймов, времени `checks`; матрица совместимости на трёх ОС (R4.2); отчёт о времени релиза против цели ≤20 минут. Репетицию релиза до canary (R5.1) запускать только с явного разрешения автора.

### 10.6 Регрессия архитектуры и приёмка
Сделать: CI-проверка, что линты границ/именования не получили новых исключений, каталог ошибок без кодов без подсказки; чек-лист приёмки автора (ежедневное использование, срок оговорить); закрыть все `?` в `docs/architecture/target/`, перевести документы из DRAFT и слить в `docs/architecture/`.
После: 10.1–10.5.
